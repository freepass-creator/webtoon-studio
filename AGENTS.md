# Webtoon Studio 공통 작업 매뉴얼

이 파일은 저장소 전체에 적용되는 최상위 작업 규칙이다. 모든 Codex 세션과 작업자는 웹툰 관련 파일을 만들거나 수정하기 전에 먼저 읽고 따른다. 하위 폴더의 `AGENTS.md`는 이 규칙에 프로젝트별 내용을 추가하며, 이 공통 규칙을 없애지 않는다.

## 1. 기본 열람 방식

- 사용자가 받는 기본 결과물은 **GitHub에서 바로 렌더링되는 Markdown 뷰어**다.
- 작품 홈은 각 작품 폴더의 `README.md`, 회차는 `episodes/EP-XX/README.md` 또는 기존 회차 이미지 폴더의 `README.md`, 캐스팅은 `CASTING.md`를 사용한다.
- 사용자에게는 폴더(`tree`) 링크나 로컬 주소가 아니라, 내용이 즉시 보이는 `blob/main/.../README.md` 또는 `blob/main/.../CASTING.md` 링크를 준다.
- GitHub Pages, Vercel, ChatGPT Site, 별도 HTML 배포는 사용자가 명시적으로 요청할 때만 사용한다.
- 정적 HTML이 이미 있어도 GitHub Markdown 뷰어를 기본 바로보기 경로로 유지한다.

## 2. 화면 구조

모든 작품은 아래 순서를 유지한다.

1. 루트 `README.md`: 전체 작품 목록
2. 작품 `README.md`: 작품 소개와 회차·캐스팅 목록
3. 회차·캐스팅 Markdown: 이미지가 위에서 아래로 바로 펼쳐지는 실제 열람 화면

- 한 화면에는 제목, 필요한 한 줄 설명, 이미지와 이동 링크만 둔다.
- 개발 명령어, 내부 프롬프트, 파일 구조, 빌드 로그는 사용자용 뷰어에 넣지 않는다.
- 컷 사이에 불필요한 설명이나 파일명을 노출하지 않는다.
- 이미지는 모바일 폭에 맞춰 자동 축소되는 GitHub 기본 렌더링을 사용한다.

## 3. 뒤로가기와 이동

- 작품 홈 맨 위에는 반드시 `[← 전체 작품](../README.md)`처럼 루트로 돌아가는 링크를 둔다.
- 회차·캐스팅 화면 맨 위와 맨 아래에는 반드시 `[← 작품명](../README.md)`처럼 작품 홈으로 돌아가는 링크를 둔다.
- 회차 하단에는 가능한 경우 `← 이전 화`와 `다음 화 →`를 함께 둔다.
- 링크는 한 번 눌러 바로 이동해야 하며 중간 리디렉션, 로그인 전용 주소, 죽은 링크를 사용하지 않는다.
- 이미지 원본을 연 뒤 휴대전화의 뒤로가기 버튼을 누르면 같은 뷰어로 복귀하도록, 이미지는 현재 화면에서 raw 원본 링크로 연결한다.

## 4. 이미지 표시 형식

각 이미지는 화면에 바로 보이고, 누르면 원본이 열리게 한다.

```md
[![EP.01 컷 01](scene-01.jpg)](https://raw.githubusercontent.com/OWNER/REPO/main/작품/회차/scene-01.jpg)
```

- 미리보기 경로는 Markdown 파일 기준 상대경로를 사용한다.
- 클릭 대상은 `raw.githubusercontent.com` 원본 링크를 사용한다.
- 세로 웹툰은 컷을 이야기 순서대로 한 장씩 연속 배치한다.
- 캐스팅 보드는 후보 코드, 핵심 스펙, 후보표 이미지만 간결하게 표시한다.
- 이미지 파일명은 정렬 가능한 번호를 포함한다: `scene-00`, `scene-01`, `scene-02`.
- 이미지나 수정본은 GitHub에 먼저 커밋한 뒤 뷰어에 연결한다.

## 5. 새 작품 등록

새 작품을 만들 때 한 번에 아래를 처리한다.

1. 작품 폴더와 작품 `README.md` 생성
2. 루트 `README.md`의 전체 작품 목록에 바로보기 링크 추가
3. 회차 또는 캐스팅 Markdown에 상단·하단 뒤로가기 추가
4. 모든 이미지가 GitHub 화면에서 실제로 렌더링되는지 확인
5. 사용자에게 검증된 GitHub Markdown 링크 하나를 제공

## 6. 완료 기준

- 링크를 누르면 설명 페이지가 아니라 이미지가 같은 화면에 즉시 보인다.
- 위에서 아래로 자연스럽게 스크롤된다.
- 상단과 하단에서 한 번에 작품 목록으로 돌아갈 수 있다.
- 사진을 누르면 원본이 열리고 휴대전화 뒤로가기로 뷰어에 복귀한다.
- 저장소의 루트 첫 화면에서 모든 작품을 찾을 수 있다.

## 7. 공통 캐스팅 절대규격

- 캐릭터 후보·확정본·의상 테스트를 만들기 전에 루트 [`CASTING-STANDARD.md`](CASTING-STANDARD.md)를 반드시 읽는다.
- 모든 작품은 `assets/casting/`의 공통 프로필·의상 템플릿을 사용한다.
- 캔버스, 0~200cm 눈금, 발바닥 기준선, 픽셀 배율은 작품별로 바꾸지 않는다.
- 생성 모델이 그린 눈금만으로 절대 키를 판정하지 않는다. 인물을 분리한 뒤 공통 템플릿 위에서 실제 픽셀 높이를 맞춘다.
- 작품별 규격은 배역·의상·표정·연기 조건만 추가할 수 있으며 공통 절대값을 덮어쓸 수 없다.
