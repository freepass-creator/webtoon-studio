# Webtoon Studio 공통 작업 매뉴얼

이 파일은 저장소 전체에 적용되는 최상위 작업 규칙이다. 모든 Codex 세션과 작업자는 웹툰 관련 파일을 만들거나 수정하기 전에 먼저 읽고 따른다. 하위 폴더의 `AGENTS.md`는 이 규칙에 프로젝트별 내용을 추가하며, 이 공통 규칙을 없애지 않는다.

## 1. 기본 열람 방식

- 사용자가 받는 기본 결과물은 **GitHub에서 바로 렌더링되는 Markdown 뷰어**다.
- 작품 홈은 각 작품 폴더의 `README.md`, 회차는 회차 폴더의 `README.md`, 캐스팅은 `CASTING.md`를 사용한다.
- 사용자에게는 폴더 링크나 로컬 주소가 아니라 내용이 즉시 보이는 `blob/main/...` 링크를 준다.
- GitHub Pages, Vercel, ChatGPT Site, 별도 HTML 배포는 사용자가 명시적으로 요청할 때만 사용한다.

## 2. 화면 구조

1. 루트 `README.md`: 전체 작품 목록
2. 작품 `README.md`: 작품 소개와 회차·캐스팅 목록
3. 회차·캐스팅 Markdown: 이미지가 위에서 아래로 바로 펼쳐지는 실제 열람 화면

- 사용자용 화면에는 제목, 필요한 한 줄 설명, 이미지와 이동 링크만 둔다.
- 내부 프롬프트, 파일 구조, 빌드 로그는 노출하지 않는다.
- 이미지는 모바일 폭에 맞춰 자동 축소되는 GitHub 기본 렌더링을 사용한다.

## 3. 뒤로가기와 이동

- 작품 홈 맨 위에는 루트 작품 목록으로 돌아가는 링크를 둔다.
- 회차·캐스팅 화면 맨 위와 맨 아래에는 작품 홈 또는 캐스팅 목록으로 돌아가는 링크를 둔다.
- 회차 하단에는 가능한 경우 이전 화와 다음 화 이동을 함께 둔다.
- 이미지 원본을 연 뒤 휴대전화 뒤로가기로 같은 뷰어에 복귀하도록 이미지는 raw 원본 링크로 연결한다.

## 4. 이미지 표시 형식

```md
[![사진 설명](image.png)](https://raw.githubusercontent.com/OWNER/REPO/main/경로/image.png)
```

- 미리보기는 Markdown 기준 상대경로, 클릭 대상은 `raw.githubusercontent.com` 원본을 사용한다.
- 이미지와 수정본은 GitHub에 먼저 커밋한 뒤 뷰어에 연결한다.
- 사용자가 보는 화면에는 최종 또는 검토 대상 이미지만 간결하게 펼친다.

## 5. 새 작품 등록

1. 작품 폴더와 작품 `README.md` 생성
2. 루트 `README.md` 작품 목록에 바로보기 링크 추가
3. 회차 또는 캐스팅 Markdown에 위·아래 뒤로가기 추가
4. 모든 이미지가 실제로 렌더링되는지 확인
5. 사용자에게 검증된 GitHub Markdown 링크 하나 제공

## 6. 완료 기준

- 링크를 누르면 이미지가 같은 화면에 즉시 보인다.
- 위에서 아래로 자연스럽게 스크롤된다.
- 상단과 하단에서 한 번에 이전 목록으로 돌아갈 수 있다.
- 사진을 누르면 원본이 열리고 휴대전화 뒤로가기로 뷰어에 복귀한다.

## 7. 공통 캐스팅 규격

- 캐릭터 후보·확정본을 만들기 전에 루트 [`CASTING-STANDARD.md`](CASTING-STANDARD.md)를 반드시 읽는다.
- 공식 캐스팅 자료는 **배우 한 명당 한 장짜리 수영복 바디 프로필**로 통일한다.
- 한 장 안에 정면·옆면·뒷면, 0~200센티미터 키 눈금, 후보명, 나이·키·몸무게를 넣는다.
- 얼굴 표정표, 의상 룩북, 자세·동작표는 사용자가 별도로 요청하지 않는 한 만들거나 공식 화면에 올리지 않는다.
- 후보별 `README.md`에는 공식 바디 프로필 한 장만 펼친다.
