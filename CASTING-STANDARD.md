# Webtoon Studio 공통 캐스팅 절대규격

버전: `CASTING-ABS-v1.0`  
적용 범위: 이 저장소의 모든 웹툰과 모든 성인 캐릭터  
원칙: **한 명씩, 같은 화면·같은 척도·같은 촬영 조건으로 만든 뒤 절대값으로 비교한다.**

## 1. 단일 기준

- 이 문서와 `assets/casting/`의 템플릿을 저장소 전체의 유일한 공통 캐스팅 기준으로 사용한다.
- 작품별 `AGENTS.md`와 `CASTING.md`에는 배역별 의상·표정·연기 조건만 추가한다.
- 작품별 문서가 캔버스, 키 눈금, 배율, 기준선, 파일 순서를 임의로 바꾸지 않는다.
- 기존 후보도 재검토하거나 확정할 때 이 절대규격으로 다시 만든다.

## 2. 배우 1명당 필수 결과

1. `casting-profile.jpg` — 얼굴 프로필 3컷과 절대척도 전신 정면·측면·후면을 합친 기본 배우 프로필
2. `face-reference.jpg` — 정면 얼굴 정체성 기준 원본
3. `body-turnaround.jpg` — 체형 확인용 정면·정확한 측면·후면
4. `costume-test.jpg` — 같은 배우의 3가지 착장 비교
5. `pose-test.jpg` — 정면 서기·걷기·앉기 또는 배역별 핵심 자세
6. `expression-test.jpg` — 무표정과 작품별 핵심 감정
7. `chemistry-test.jpg` — 상대 배우와의 키 차이·시선·관계 연기
8. `profile.md` — 나이, 키, 체중, 체형, 얼굴, 승인 상태
9. `prompt.md` — 생성 프롬프트와 수정 이력
10. `casting.json` — 템플릿 버전과 절대 배율 수치

아직 만들지 않은 테스트는 파일을 비워 두지 말고 `profile.md`에 `미제작`으로 표시한다.

## 3. 절대척도 프로필 보드

- 템플릿: `assets/casting/casting-profile-template-v1.png`
- 편집 원본: `assets/casting/casting-profile-template-v1.svg`
- 캔버스: **2400 × 3200 px** 고정
- 키 눈금: **0~200cm**, 10cm마다 가는 선, 50cm마다 굵은 선
- 환산값: **1cm = 10px**
- 바닥 0cm 기준선: **y=2300px**
- 200cm 기준선: **y=300px**
- 인물 픽셀 높이: `프로필 키(cm) × 10px`
  - 168cm 배우 = 정수리부터 발바닥까지 1680px
  - 180cm 배우 = 정수리부터 발바닥까지 1800px
- 모든 전신은 발바닥을 0cm 선에 정확히 맞춘다.
- 신발·가체·장식 높이는 신체 키에 포함하지 않는다. 기본 체형 보드는 맨발 기준이다.
- 정면, 왼쪽 측면, 후면 세 인물을 같은 크기로 놓고 머리와 발을 자르지 않는다.

## 4. 촬영과 합성 고정값

- 왜곡이 적은 70~85mm 상당의 전신 촬영 느낌
- 카메라 높이 100cm, 수평 0°, 기울임 없음
- 광각 원근감, 로우앵글, 하이앵글 금지
- 같은 조명, 같은 배경색, 같은 화이트밸런스
- 체형 보드는 3개의 동일한 세로 구획에 정면·측면·후면을 배치한다.
- 중립 자세를 사용하고 팔과 다리가 서로 가리지 않게 배치한다.
- 생성 이미지 속 눈금이나 글자를 그대로 믿지 않는다.
- 인물은 배경에서 분리한 뒤 템플릿 위에 후처리로 배치하고, 키 수치에 맞춰 픽셀 높이를 조정한다.
- 키 눈금, 후보 코드, 신체 수치는 생성 모델이 아니라 편집 단계에서 정확한 글자로 넣는다.

## 5. 기본 체형 의상

- 관계·연기 장면에 참여하는 인물은 모두 명시적인 성인이다.
- 체형 확인은 불투명한 흰색 기본 수영복 또는 역할에 맞는 동일한 흰색 캐스팅복으로 통일한다.
- 사극 여성 배역은 불투명한 미색 가슴싸개와 무릎 위 길이 속바지를 공통 캐스팅복으로 사용할 수 있다.
- 같은 작품의 같은 배역군 후보들은 옷의 형태·길이·색을 완전히 동일하게 유지한다.
- 맨발, 중립 표정, 장신구 없음, 선정적인 포즈 없음.
- 후보 간 의상 면적과 형태를 바꾸지 않는다.
- 몸매 보정, 키 늘리기, 얼굴 축소, 다리 연장 등 후보별 임의 보정을 금지한다.

## 6. 얼굴 프로필

- 정면, 좌우 45도 중 한 방향, 정확한 측면 3컷
- 같은 얼굴 ID, 같은 헤어, 같은 조명, 같은 크롭
- 기본은 무표정이며 미소·불안·분노·유혹 등은 `expression-test.jpg`에서 비교한다.
- 실제 배우를 그대로 복제하지 않고 가상의 성인 배우로 제작한다.

## 7. 의상 테스트

- 템플릿: `assets/casting/costume-test-template-v1.png`
- 편집 원본: `assets/casting/costume-test-template-v1.svg`
- 캔버스·키 눈금·바닥선·배율은 기본 프로필 보드와 완전히 동일하다.
- 세 착장 모두 같은 얼굴, 같은 체형, 같은 키, 같은 카메라를 유지한다.
- 작품별로 LOOK 01~03의 종류만 정한다.
- 포즈 때문에 체형 비교가 흐려지지 않도록 의상 보드에서는 정면 중립 자세를 우선한다.

## 8. 포즈·표정·케미스트리

- 포즈 테스트는 의상 보드와 별도 파일로 만든다.
- 표정 테스트는 얼굴 크롭과 카메라를 고정하고 표정만 바꾼다.
- 2인 케미스트리 테스트도 두 배우의 절대 키를 `1cm=10px`로 맞추고 같은 0cm 바닥선에 세운다.
- 키 차이를 장면 연출을 위해 임의로 늘리거나 줄이지 않는다.
- 앉기·무릎 꿇기 장면은 별도 포즈 테스트이며 기본 키 비교 보드를 대체하지 않는다.

## 9. 후보 저장 구조

```text
작품/characters/candidates/후보코드/
  casting-profile.jpg
  face-reference.jpg
  body-turnaround.jpg
  costume-test.jpg
  pose-test.jpg
  expression-test.jpg
  chemistry-test.jpg
  profile.md
  prompt.md
  casting.json
```

`casting.json` 예시:

```json
{
  "template": "CASTING-ABS-v1.0",
  "canvas_px": [2400, 3200],
  "scale_px_per_cm": 10,
  "floor_y_px": 2300,
  "height_cm": 168,
  "body_height_px": 1680,
  "views": ["front", "left-profile", "back"]
}
```

## 10. 승인 순서

1. 얼굴 프로필 승인
2. 절대척도 체형 보드 승인
3. 의상 테스트 승인
4. 포즈·표정 테스트 승인
5. 상대 배우와 케미스트리 승인
6. 확정 후보를 `characters/main/`에 캐릭터 바이블로 등록
7. 확정 뒤에만 에피소드 제작

모든 결과는 GitHub에 먼저 저장하고, 캐스팅 화면에서는 사진이 바로 보이며 누르면 raw 원본이 열리게 한다.

## 11. 후보별 세로 열람 화면

- 후보마다 가상의 한글 이름을 부여하고 후보 번호와 함께 표시한다.
- 후보 폴더의 `README.md`를 한 화면에서 아래로 계속 보는 정식 캐스팅 화면으로 사용한다.
- 표시 순서는 **대표 프로필 → 절대척도 체형·키 → 정면·옆면·뒷면 → 얼굴 기준·표정 → 의상 → 자세·동작 → 상대 배우 호흡**으로 고정한다.
- 각 사진은 화면에 바로 펼쳐지고, 누르면 원본이 열리며 휴대전화 뒤로가기로 같은 후보 화면에 복귀해야 한다.
- 화면 맨 위와 맨 아래에는 작품 캐스팅 목록으로 돌아가는 링크를 둔다.
- 사진 안의 제목·의상·자세·표정 명칭과 사용자용 화면 문구는 한글로 쓴다. 내부 파일명과 기계 판독용 자료의 키만 예외로 한다.

